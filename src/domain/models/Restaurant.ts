export class Restaurant {
  name: string;
  address: string;
  capacity: number;

  constructor(name: string,address: string,capacity: number) {
    this.name = name;
    this.address = address;
    this.capacity = capacity;
  }
}
